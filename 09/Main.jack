class Main {
    function void main() {
        var Game game;
        var boolean run;
        let game = Game.new(10);
        let run = true;
        while (run) {
        	let run = game.run();
        }
        do game.dispose();
        return;
    }
}
